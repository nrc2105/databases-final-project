/*
 * Not exactly sure what I want out of this one,
 * but it must have a 'requiredTables()' method
 * that returns a collection of the tables that will
 * be directly read or written to (the Dbms will find the
 * the paths from that) and a nextInstruction() method
 * that returns null when it's done.  
 */
